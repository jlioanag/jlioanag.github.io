---
import { Icon } from "astro-icon/components";

const { nav, text } = Astro.props;
---

<a
    href={nav}
    class="text-gray-500 hover:text-gray-700 transition-colors duration-200 inline-flex items-center"
    >{text} <Icon name="tabler:arrow-up-right" class="mb-1" /></a
>
